<style>
.checklist-box {
  display: block;
  height: 14em;
  font-weight: bold;
}
.checklist-title {
  text-align: center;
  color: white;
  font-size: 1em;
  padding: 0.5em;
}
.checklist-title-box {
  background-color: #58AA4D;
  border-radius: 4px;
}
.checklist-item {
  text-align: center;
  margin: 0.8em;
}
.checklist-item:hover {
  font-weight: 400;
}
</style>

<div class="checklist-title-box">
  <h4 class="centered checklist-title">Account Setup Checklist</h4>
</div>

<div class="checklist-box">

  <%= link_to anchor: 'application' do %>
    <p class="checklist-item <%= @tutor.application_status == 'Complete' ? 'strikeout-text' : nil %>">Complete your application</p>
  <% end %>

  <%= link_to home_tutor_path(@tutor.slug) do %>
    <p class="checklist-item">Receive application approval</p>
  <% end %>

  <%= link_to tutor_courses_path(@tutor.slug) do %>
    <p class="checklist-item">Approve courses and rates</p>
  <% end %>

  <%= link_to schedule_tutor_path(@tutor.slug) do %>
    <p class="checklist-item">Set your availability</p>
  <% end %>

  <%= link_to payment_info_tutor_path(@tutor.slug) do %>  
    <p class="checklist-item">Link your bank account</p>
  <% end %>

</div>