<!-- this is an empty layout file so that we can have AJAX filled modals which load as separate pages, without the header and footer from the normal base layout -->
<% if @modal == 'regular_times' %>
  <%= render '/views/checkout/regular_times' %>
<% end %>