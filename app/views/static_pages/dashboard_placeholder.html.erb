<h1>Dashboard Placeholder</h1>

<p>This is just to serve as a page to render on succesfull sign up and sign-in for current testing purposes</p>

<p><%= "Tutor ID: #{@tutor.id}" %></p>

<p><%= image_tag @tutor.transcript.url(:large) %></p>

<p><%= "Extra Info: #{@tutor.extra_info}" %></p>

<p>Courses</p>

<table>
  <tr>
    <th>School</th>
    <th>Subject</th>
    <th>Call Number</th>
    <th>Course Name</th>
    <th>Rate</th>
  </tr>
  <tr>
  <% @tutor.tutor_courses.each do |tutor_course| %>
    <td><%= tutor_course.course.school_id %></td>
    <td><%= tutor_course.course.subject_id %></td>
    <td><%= tutor_course.course.call_number %></td>
    <td><%= tutor_course.course.friendly_name %></td>
    <td><%= tutor_course.rate %></td>
  <% end %>
  </tr>
</table>