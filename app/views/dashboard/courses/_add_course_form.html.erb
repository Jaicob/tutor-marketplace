<div class="row">
  <%= form_for @tutor_course do |f| %>
    <%= fields_for :course do |course| %>
      <%= react_component('CourseBox', {
              courseList: true,
              submitButton: true,
              tutor_id: @tutor.id
            }, {
              :prerender => false
            }
          ) %>
    <% end %>
  </div>
<% end %>
</div>