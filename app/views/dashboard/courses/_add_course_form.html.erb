<div class="row">
  <%= form_for @tutor_course do |f| %>
    <%= fields_for :course do |course| %>
      <%= react_component('CourseBox', {
              courseList: false,
              submitButton: false,
              tutor_id: @tutor.id
            }, {
              :prerender => false
            }
          ) %>
    <% end %>
    <%= f.submit 'Add course', class: 'button radius success tiny right' %>
    <%= hidden_field_tag(:tutor_id, value = @tutor.id) %>
  </div>
<% end %>
</div>