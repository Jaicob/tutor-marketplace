<section class="secondary-content main" role="main">
  <div class="wrapper">

    <div class="g g-3up">
      <div class="gi">
        <h3>Create a New Promotion</h3>
      </div>
      <div class="gi">
      </div>
      <div class="gi">
        <%= link_to 'All Promotions', new_admin_promotion_path, class: 'btn' %>
      </div>
    </div>

    <div class="g g-1up">
      <div class="gi">
        <%= form_for :promotion, url: admin_promotions_path do |f| %>
          <%= f.label :category %>
          <%= f.select(:category,
            options_for_select([
              ['Free from Axon', :free_from_axon],
              ['Free from Tutor', :free_from_tutor], 
              ['Percent off from Axon', :percent_off_from_axon],
              ['Percent off from Tutor', :percent_off_from_tutor],
              ['Dollar Amount off from Axon', :dollar_amount_off_from_axon],
              ['Dollar Amount off from Tutor', :dollar_amount_off_from_tutor],
              ['Repeating Percent off from Tutor', :repeating_percent_off_from_tutor], 
              ['Repeating Dollar Amount off from Tutor', :repeating_dollar_amount_off_from_tutor]
            ]), include_blank: true)  %>

          <%= f.label :amount %>
          <%= f.number_field :amount %>

          <%= f.label :valid_from %>
          <%= f.date_select :valid_from %>

          <%= f.label :valid_until %>
          <%= f.date_select :valid_until %>

          <%= f.label :redemption_limit %>
          <%= f.number_field :redemption_limit %>

          <%= f.label :description %>
          <%= f.text_area :description %>
          
          <%= f.submit 'Create Promotion' %>
        <% end %>
      </div>
    </div>

  </div>
</section>